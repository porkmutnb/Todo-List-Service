spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      data-locations: classpath:db/data-dev.sql

logging:
  level:
    org.springframework.jdbc.datasource.init: DEBUG
